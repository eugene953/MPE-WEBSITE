pipeline {
    // The Jenkins controller/agent in this environment runs on Windows.
    // Docker's Windows workflow plugin passes a Windows workspace path to
    // the Linux container, which Alpine cannot use as its working directory.
    agent any
    stages {
        stage('verify Node.js') {
            steps {
                bat 'node --version'
            }
        }
    }
}
